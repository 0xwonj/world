# Pre-Redesign Baseline Evidence

## Provenance

```text
captured: 2026-07-27
source branch: main
source commit: 4bc71b7
origin relation: origin/main, zero commits ahead or behind
preserved WIP branch: codex/preserve-pre-redesign-2026-07-27
preserved WIP commit: 6571dc8442f4450e067d60c8a2a71257370512df
```

This evidence describes the clean committed implementation at `4bc71b7`, not
the additional WIP preserved at `6571dc8`.

## Baseline verification

The following commands passed without repair:

```text
cargo fmt --all --check
cargo check --workspace
cargo clippy --workspace --all-targets
cargo test --workspace
git diff --check
```

`cargo test --workspace` executed 155 passing tests and zero failures.

Passing legacy tests establish a regression reference only. They do not make
the current authority or decision architecture conform to the target.

## Selected package graph

| Package | Direct local dependencies |
|---|---|
| `world-core` | — |
| `world-defs` | `world-core` |
| `world-model` | `world-core`, `world-defs` |
| `world-runtime` | `world-core`, `world-defs`, `world-model` |
| `world-context` | `world-core`, `world-defs`, `world-model` |
| `world-decision` | `world-core`, `world-defs`, `world-context` |
| `world-authoring` | `world-core`, `world-defs` |
| `world-engine` | `world-authoring`, `world-context`, `world-core`, `world-decision`, `world-defs`, `world-model`, `world-runtime` |
| `world-standard` | `world-core`, `world-defs` |
| `world-standard-runtime` | `world-core`, `world-defs`, `world-model`, `world-runtime`, `world-standard` |

The target-invalid direct edge is `world-engine -> world-authoring`.

## Superseded authority and pipeline symbols

| Symbol | Baseline location | Target disposition |
|---|---|---|
| `WorldModel` | `crates/world-model/src/model.rs` | delete mutable aggregate; replace with immutable model records and private runtime head |
| `WorldModel::apply_hard_commit` | `crates/world-model/src/model.rs` | delete; publication belongs to runtime |
| `WorldModel::apply_runtime_control_update` | `crates/world-model/src/model.rs` | delete; publication belongs to runtime |
| `CausalRuntime` | `crates/world-runtime/src/runtime.rs` | delete; replace with opaque runtime service and attempt driver |
| `DecisionRunner` | `crates/world-decision/src/runner/mod.rs` | delete without adapter; rebuild explicit lifecycle ports later |

M1 must additionally remove the old accepted commit/update authority model,
durable numeric definition identity, omnibus version anchor, broad context
pipeline, and source-scan authority allowlist before its target-state merge.

## Preservation verification

The preservation snapshot contains 78 changed paths and 23,476 inserted lines.
It includes all selected tracked and untracked source, architecture, research,
review, and plan files. Ignored `target/` build output is absent.

The preservation commit was checked with:

```text
git diff --cached --check
git status --short --branch
git show --stat --oneline --summary 6571dc8
git ls-tree -r --name-only 6571dc8 -- docs/architecture/target-architecture
git fsck --no-dangling --no-progress
```
